@using Microsoft.AspNetCore.Identity;
@using ELearningV2.Models;
@inject SignInManager<User> signInManager;
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<div class="collapse navbar-collapse" id="collapsibleNavbar">
    <ul class="navbar-nav ml-auto">
        @*If the user is signed-in display Logout link*@
        @if (signInManager.IsSignedIn(User))
        {
            <li class="nav-item">
                <form method="post">
                    <button type="submit" style="width:auto"
                            class="nav-link btn btn-link py-0" asp-action="Logout" asp-controller="Users">
                        Logout @User.Identity.Name
                    </button>
                </form>
            </li>
        }
        else
        {
            <li class="nav-item">
                <a class="nav-link" asp-action="Register" asp-controller="Users">
                    Register
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" asp-action="Login" asp-controller="Users">
                    Login
                </a>
            </li>
        }
    </ul>
</div>
@RenderBody();
